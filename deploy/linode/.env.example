PHX_HOST=example.com
SECRET_KEY_BASE=replace-with-mix-phx-gen-secret
PORT=4000
PHX_SERVER=true
ENABLE_DISTRIBUTED_ERLANG=false

# Single-node deployment works as-is.
# Multi-node requires node-aware room routing or externalized state.
RELEASE_COOKIE=replace-with-shared-cookie
RELEASE_NODE=
DNS_CLUSTER_QUERY=
